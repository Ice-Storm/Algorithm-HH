#include <stdio.h>
#include <malloc.h>

#define MAXSIZE 41

struct list{
	int data;
	int cur;
};

int iniNodeList(list NodeList);

int main(int argc, char *argv[])
{
	struct list cirNodeList[1];
	
	
	
	iniNodeList(list cirNodeList[]);
	return 0;
}

int iniNodeList(list NodeList) {
	
	for (int i = 0; i < MAXSIZE; i++) {
	//	NodeList[i].data = i;
	}
	
	return 0;	
}
